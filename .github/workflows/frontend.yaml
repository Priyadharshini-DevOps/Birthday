name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  deploy-frontend:

    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_ROLE }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Docker Image
        run: |
          docker build -t birthday-app-frontend ./frontend

      - name: Tag Docker Image
        run: |
          docker tag birthday-app-frontend:latest ${{ secrets.FRONTEND_ECR }}:latest

      - name: Push Docker Image
        run: |
          docker push ${{ secrets.FRONTEND_ECR }}:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster ${{ secrets.ECS_CLUSTER }} \
            --service ${{ secrets.FRONTEND_SERVICE }} \
            --force-new-deployment

      - name: Wait for Deployment
        run: |
          aws ecs wait services-stable \
            --cluster ${{ secrets.ECS_CLUSTER }} \
            --services ${{ secrets.FRONTEND_SERVICE }}

      - name: Deployment Complete
        run: echo "Frontend deployment completed successfully!"